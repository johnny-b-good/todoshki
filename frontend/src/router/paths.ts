// App
// -----------------------------------------------------------------------------
import { RouteName } from "./RouteName";

export const paths: Record<RouteName, string> = {
  root: "/",
  boardList: "/boards",
};
